require 'spec_helper'

describe Omniauth::Todoist do
  it 'has a version number' do
    expect(Omniauth::Todoist::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
